import { Text } from "@chakra-ui/react";

export function DataPoint({ value }: { value: number }) {
    return <Text color="green.100">{value}</Text>;
}

export function CumulativeWinningsDataPoint({ value }: { value: number }) {

}

export function VPIPDataPoint({ value }: { value: number }) {

}
