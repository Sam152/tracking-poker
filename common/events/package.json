{
  "name": "tp-events",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --platform=node --target=node20 src/index.ts --bundle --outfile=build/index.js && tsc src/index.ts --emitDeclarationOnly --declaration --outDir build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.552.0",
    "aws-xray-sdk": "^3.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5"
  }
}
